import SwiftUI
import AVFoundation

// Placeholder view until WebRTC is properly set up
struct RTCVideoView: View {
    var body: some View {
        Color.black
            .overlay {
                Image(systemName: "video.slash")
                    .font(.largeTitle)
                    .foregroundColor(.white)
            }
    }
} 